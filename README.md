# คู่มือการติดตั้งและใช้งานเว็บไซต์ "เที่ยววัดชลบุรี" (Travel Wat Chonburi)

เว็บไซต์แนะนำสถานที่ท่องเที่ยวเชิงวัฒนธรรมและวัดวาอารามในจังหวัดชลบุรี พัฒนาด้วย PHP, MySQL และตกแต่งสวยงามด้วย Tailwind CSS

---

## 📋 สิ่งที่ต้องเตรียม (Prerequisites)

1.  **XAMPP** (หรือโปรแกรมจำลอง Server อื่นๆ ที่รองรับ PHP และ MySQL)
2.  **Web Browser** (Chrome, Edge, Firefox ฯลฯ)
3.  **Text Editor** (VS Code, Notepad++ หรืออื่นๆ สำหรับแก้ไขโค้ดถ้าจำเป็น)

---

## ⚙️ ขั้นตอนการติดตั้ง (Installation)

### 1. ติดตั้งไฟล์เว็บไซต์
*   นำโฟลเดอร์โปรเจกต์ (เช่น `myproject` หรือ `temple_chonburi`) ไปวางไว้ในโฟลเดอร์ `htdocs` ของ XAMPP
    *   *Path ปกติคือ:* `C:\xampp\htdocs\myproject\`

### 2. เริ่มการทำงาน Server
*   เปิดโปรแกรม **XAMPP Control Panel**
*   กดปุ่ม **Start** ที่โมดูล **Apache** และ **MySQL** ให้ขึ้นสีเขียว

### 3. ติดตั้งฐานข้อมูล (Database Setup)
*   เปิดเบราว์เซอร์แล้วเข้าไปที่ [http://localhost/phpmyadmin](http://localhost/phpmyadmin)
*   สร้างฐานข้อมูลใหม่ (New Database):
    *   ตั้งชื่อว่า: `chonburi_temples`
    *   เลือก Collation เป็น: `utf8_general_ci` (เพื่อรองรับภาษาไทย)
    *   กด **Create**
*   นำเข้าข้อมูล (Import):
    1.  คลิกที่ฐานข้อมูล `chonburi_temples` ที่เพิ่งสร้างทางซ้ายมือ
    2.  ไปที่แท็บ **Import** ด้านบน
    3.  กด **Choose File** แล้วเลือกไฟล์ `database.sql` (อยู่ในโฟลเดอร์โปรเจกต์) -> กด **Go** (เพื่อสร้างตาราง temple)
    4.  กด **Choose File** อีกครั้ง แล้วเลือกไฟล์ `create_messages_table.sql` -> กด **Go** (เพื่อสร้างตาราง messages สำหรับฟอร์มติดต่อ)

### 4. เชื่อมต่อฐานข้อมูล (Optional)
*   หากคุณตั้งรหัสผ่าน MySQL ไว้ หรือใช้ User อื่นที่ไม่ใช่ `root`
*   ให้เปิดไฟล์ `includes/db.php` แล้วแก้ไขส่วนนี้:
    ```php
    $username = "root";    // ชื่อผู้ใช้ (ปกติ XAMPP คือ root)
    $password = "";        // รหัสผ่าน (ปกติ XAMPP ว่างไว้)
    $dbname = "chonburi_temples";
    ```

### 5. เสร็จสิ้น! 🎉
*   เข้าใช้งานเว็บไซต์ได้ที่: [http://localhost/myproject](http://localhost/myproject) (หรือตามชื่อโฟลเดอร์ที่คุณตั้ง)

---

## 📖 คู่มือการใช้งาน (User Manual)

### 1. การเพิ่ม/ลบ/แก้ไข ข้อมูลวัด
เนื่องจากระบบไม่มีหน้า Admin หลังบ้าน การจัดการข้อมูลจะทำผ่าน **phpMyAdmin**:
1.  เข้า [http://localhost/phpmyadmin](http://localhost/phpmyadmin)
2.  เลือกตาราง `temples`
3.  **เพิ่มข้อมูลใหม่**: กดแท็บ **Insert** (แทรก) แล้วกรอกข้อมูล:
    *   `name`: ชื่อวัด
    *   `image`: path รูปภาพ (เช่น `assets/img/wat.jpg` - *ต้องเอารูปไปวางจริงด้วย*)
    *   `address`: ที่อยู่
    *   `description`: รายละเอียด
    *   กด **Go** เพื่อบันทึก
4.  **แก้ไข/ลบ**: กดแท็บ **Browse** (เปิดดู) แล้วกดปุ่ม Edit หรือ Delete ที่รายการที่ต้องการ

### 2. การจัดการแบบฟอร์มติดต่อ (Contact Form)
เมื่อผู้ใช้ส่งข้อความผ่านหน้า "ติดต่อเรา":
1.  ระบบจะบันทึกข้อความลงในตาราง `messages` ในฐานข้อมูล
2.  คุณสามารถเข้าไปดูข้อความทั้งหมดได้ที่ phpMyAdmin -> เลือกตาราง `messages`

### 3. ฟีเจอร์หน้าเว็บไซต์
*   **หน้าแรก**: มี Slider แนะนำวัดแบบสุ่ม
*   **แนะนำวัด**: แสดงรายการวัดแบบ Grid พร้อมช่องค้นหา
*   **รายชื่อวัด**: แสดงตารางรายชื่อ พร้อมระบบแบ่งหน้า (Pagination)
*   **แผนที่**: เลือกวัดจากเมนูซ้ายเพื่อดูตำแหน่งบน Google Maps ได้ทันที

---

## 📂 โครงสร้างไฟล์ (File Structure)

*   `index.php` - หน้าแรก
*   `wat1.php` - หน้าแสดงการ์ดแนะนำวัด + ค้นหา
*   `list.php` - หน้ารายชื่อแบบตาราง + ค้นหา + แบ่งหน้า
*   `map.php` - หน้าแผนที่ Dynamic
*   `contact.php` - หน้าติดต่อเรา (เก็บลง DB)
*   `includes/` - ไฟล์ที่ใช้ร่วมกัน (Header, Footer, DB Connection)
*   `assets/` - เก็บไฟล์รูปภาพ (img) และ CSS (css)
*   `data/` - (เลิกใช้งานแล้ว เปลี่ยนไปใช้ Database แทน)

---
*จัดทำโดย: ทีมพัฒนา Travel Wat Chonburi*
